package org.stianloader.test.smatterdi;

public class SmatterDITest {
    public static void main(String[] args) {
        /*CDObjectAllocator alloc = new CDObjectAllocator();
        SimpleInjectionContextImpl inject = new SimpleInjectionContextImpl();
        InjectedObject object = new InjectedObject(8693);
        inject.setImplementation(InjectedObject.class, object);
        ExampleInjectionTarget target = alloc.allocate(MethodHandles.lookup(), ExampleInjectionTarget.class, inject);
        if (target.getInjectedObject() != object) {
            throw new AssertionError("target.getInjectedObject != object");
        }*/
    }
}
